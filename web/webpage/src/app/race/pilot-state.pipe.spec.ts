import { PilotStatePipe } from './pilot-state.pipe';

describe('PilotStatePipe', () => {
  it('create an instance', () => {
    const pipe = new PilotStatePipe();
    expect(pipe).toBeTruthy();
  });
});
